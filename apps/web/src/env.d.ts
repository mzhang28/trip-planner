/**
 * Stamped into the bundle by vite.config.ts, which replaces this name with the
 * literal at build time. `commit` is empty where the build had no repository to
 * read it from and none was passed in, which is what happens inside Docker.
 */
declare const __APP_BUILD__: { commit: string; builtAt: string };
